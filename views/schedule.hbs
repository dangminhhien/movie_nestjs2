<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.5.1/dist/jquery.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.1/dist/js/bootstrap.bundle.min.js"></script>

    <title>Chọn lịch xem phim</title>
</head>

<body>
    <!-- Nav bar -->
    {{> header }}
    <!-- End of navbar -->

    <div class="container mt-5">
        <h2>Phim đã chọn: {{movieName}}</h2>
        <h3>Địa chỉ đã chọn: {{localName}}</h3>
        
        <form action="/schedule" method="post">
            <input type="hidden" name="movieName" value="{{movieName}}">
            <input type="hidden" name="localName" value="{{localName}}">

            <div class="form-group">
                <label for="selectedDate">Chọn ngày xem phim (yyyy-MM-dd)</label>
                <input type="date" id="selectedDate" class="form-control form-control-lg" name="selectedDate" required>
            </div>

            <div class="form-group">
                <label for="selectedTime">Chọn giờ xem phim (HH:mm)</label>
                <input type="time" id="selectedTime" class="form-control form-control-lg" name="selectedTime" required>
            </div>

            <button type="submit" class="btn btn-outline-danger btn-block">Đặt lịch</button>
        </form>
    </div>

    <!-- Footer -->
    {{> footer }}

</body>
</html>
